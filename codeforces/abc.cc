#include <bits/stdc++.h>
#define MAXN 100005

using namespace std;

int arr[MAXN];
int prefix[MAXN];
int main(){
    int n;
    cin >> n;
    prefix[i] = 0;
    for(int i = 0; i < n; i++){
        cin >> arr[i];
        prefix[i]
    }


}